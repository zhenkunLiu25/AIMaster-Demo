version: '3.8'

services:
  nus-task-assistant:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    container_name: nus-task-assistant-web
    
  # Future: Add backend services
  # nus-task-backend:
  #   image: node:18-alpine
  #   working_dir: /app
  #   volumes:
  #     - ./backend:/app
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     - NODE_ENV=production
  #   restart: unless-stopped
  #   container_name: nus-task-assistant-api
  
  # Future: Add database
  # nus-task-db:
  #   image: postgres:15-alpine
  #   environment:
  #     - POSTGRES_DB=nus_tasks
  #     - POSTGRES_USER=nus_user
  #     - POSTGRES_PASSWORD=secure_password
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   restart: unless-stopped
  #   container_name: nus-task-assistant-db

# volumes:
#   postgres_data:
